---
# handlers file for ollama

- name: Restart ollama service
  ansible.builtin.systemd:
    name: ollama
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode
  tags:
    - ollama
    - ai
    - llm
    - service
