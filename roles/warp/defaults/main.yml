---
# defaults file for warp

# Warp download configuration
warp_download_url: https://app.warp.dev/get_warp?package=deb
warp_download_path: /tmp/warp-terminal.deb
warp_package_name: warp-terminal

# User configuration
warp_user: "{{ username }}"
warp_user_home: "{{ user_home }}"
